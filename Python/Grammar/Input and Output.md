# `Print` Function

1. Return a value `None`, but Python don't automatically output the None value.
```python
print(print(1), print(2))
```

output
```Python
1 # call for print(1)
2 # call for print(2)
None None # call for print()
```
