1. When using the assignment statement: `b, a = a + b, b`, where `a = 1, b = 2`，then the value of a and b is 2 and 3, for python first calculate the value of the expressions on the right, then assign them to the variables on  the left.